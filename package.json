 {
     "name": "loja-mock",
     "version": "1.0.0",
     "scripts": {
       "start": "json-server --host 0.0.0.0 --watch db.json --port ${PORT:3000}"
     },
     "dependencies": {
       "json-server": "^0.17.4"
     }
   }
